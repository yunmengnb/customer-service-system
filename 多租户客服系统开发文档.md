# 多租户客服系统开发文档

## 1. 项目概述

### 1.1 建设目标

建设一套 SaaS 多租户在线客服系统，为每个入驻用户（下文称“租户”）提供独立的客服团队、客服配置、客户账号、会话和消息数据。

系统包括三个端：

1. **平台管理员后台**：管理租户、套餐、系统配置、风控和日志。
2. **租户用户后台**：租户注册登录、创建客服、配置接待规则、查看消息并与客户聊天。
3. **客户接入界面**：客户通过专属客服链接登录或自动注册，并与对应租户的客服交流。

### 1.2 核心原则

- 所有业务数据必须按 `tenant_id` 隔离。
- 客户账号只在当前客服链接所属的接入渠道内有效，不作为全平台通用账号。
- 密码必须使用 Argon2id 或 bcrypt 哈希保存，禁止明文保存和传输。
- 聊天消息通过 WebSocket 实时推送，HTTP API 用于鉴权、配置和历史数据查询。
- 首期采用共享数据库、共享表、`tenant_id` 逻辑隔离；所有数据库查询必须自动附加租户条件。

## 2. 名词定义与角色

### 2.1 名词

| 名词 | 定义 |
|---|---|
| 平台 | 整套多租户客服 SaaS 系统 |
| 租户 | 注册并使用客服系统的企业或个人用户 |
| 坐席 | 租户创建的客服账号 |
| 接入渠道 | 一个可独立配置并生成客服链接的客户入口 |
| 客户 | 使用手机号和密码进入接入渠道聊天的人 |
| 会话 | 客户与租户客服团队之间的一次服务线程 |
| 接入 | 坐席打开或领取会话，开始处理客户消息 |

### 2.2 权限角色

| 角色 | 权限范围 |
|---|---|
| 平台超级管理员 | 管理全部租户、系统设置、套餐、全局日志和风险账号 |
| 平台运营管理员 | 按授权管理租户、工单、公告和统计，不接触客户密码 |
| 租户所有者 | 管理本租户资料、坐席、渠道、自动回复、会话和日志 |
| 租户管理员 | 管理本租户业务配置，权限由所有者分配 |
| 客服坐席 | 查看被授权渠道的会话、发送消息、使用快捷回复 |
| 客户 | 仅访问指定接入渠道及本人会话和消息 |

## 3. 业务范围

### 3.1 平台管理员后台

- 管理员登录、验证码和二次验证。
- 仪表盘：租户数、坐席数、客户数、活跃会话数、消息量。
- 租户管理：查看、启用、禁用、调整套餐和容量。
- 套餐管理：坐席上限、渠道上限、消息存储期限、附件容量。
- 系统设置：站点信息、注册开关、短信、邮件、对象存储、WebSocket 参数。
- 风控管理：异常 IP、频繁注册、登录失败、封禁记录。
- 日志审计：管理员登录日志、操作日志及租户关键操作日志。
- 数据统计：按日期、租户、渠道统计会话和消息。

### 3.2 租户用户后台

#### 账号与组织

- 租户注册、登录、找回密码、验证码。
- 企业或团队资料配置。
- 创建、编辑、启用和禁用客服坐席。
- 坐席角色及渠道权限分配。

#### 客服配置

- 创建接入渠道并设置名称、头像、品牌色、客服展示名称。
- 生成不可枚举的专属客服链接，例如：`https://chat.example.com/c/{public_token}`。
- 支持重置链接令牌；重置后旧链接立即失效。
- 配置欢迎词、关键词回复、快捷回复。
- 配置工作时间、离线提示和默认分配方式。
- 查看链接和一键复制，不在链接中暴露 `tenant_id` 或坐席 ID。

#### 消息中心

- 左侧显示会话列表，支持全部、待接入、处理中、已结束、未读筛选。
- 显示客户手机号脱敏值、最后消息、时间、未读数和会话状态。
- 点击会话进入聊天窗口并加载历史消息。
- 坐席进入会话后执行接入；客户端收到“客服已接入”系统事件。
- 坐席发送文本、图片和文件，使用快捷回复。
- 支持结束会话、重新打开和转接其他坐席。
- 多坐席同时操作时使用原子领取，避免重复接入。

### 3.3 客户接入界面

- 通过专属客服链接打开，只能进入该链接对应的渠道。
- 输入手机号、密码及验证码：
  - 当前渠道已存在该手机号账号时，验证密码并登录。
  - 当前渠道不存在时，自动创建账号并直接登录。
- 自动注册时记录注册 IP、User-Agent 和浏览器指纹哈希。
- 首次进入聊天界面时强制弹窗填写可接收消息的邮箱。
- 邮箱提交成功后才能正常发送消息。
- 进入聊天页后显示欢迎词；命中关键词时发送自动回复。
- 坐席接入后显示“客服 XXX 已接入”系统消息。
- 支持刷新或重新登录后恢复本人的历史会话。

## 4. 关键业务规则

### 4.1 客户账号作用域

根据“账号仅用于登录该客服链接窗口”的要求，客户账号唯一范围定义为：

```text
channel_id + normalized_phone
```

即同一手机号在不同客服链接中是两个相互独立的客户账号。即使两个渠道属于同一个租户，也默认不共享密码、资料和会话。

若后期需要同一租户多个渠道共享客户身份，可再将唯一范围调整为 `tenant_id + normalized_phone`，但首期不采用该方案。

### 4.2 登录与自动注册

1. 后端根据 `public_token` 查找已启用渠道。
2. 校验手机号格式、密码强度、验证码和请求频率。
3. 对手机号进行标准化，例如保存为 E.164 格式。
4. 在当前 `channel_id` 下查找手机号：
   - 已存在：校验密码，成功后建立客户会话。
   - 不存在：创建客户账号，记录注册 IP 和指纹哈希，然后建立客户会话。
5. 若客户邮箱为空，返回 `profile_required=true`，前端强制展示邮箱弹窗。
6. 邮箱保存成功后进入聊天功能。

为防止用户输错密码时意外创建新账号，是否存在账号的判断必须严格限定在当前渠道和标准化手机号上。

### 4.3 会话状态

```text
waiting（待接入）
  -> active（已接入/处理中）
  -> closed（已结束）
  -> active（重新打开）
```

- 客户首次发送消息时创建 `waiting` 会话。
- 坐席领取成功后改为 `active`，写入 `assigned_agent_id` 和 `accepted_at`。
- 接入操作通过数据库条件更新或分布式锁保证只有一个坐席领取成功。
- 客户在已结束会话中再次发消息时，可重新打开原会话或创建新会话；首期建议重新打开最近会话。

### 4.4 自动回复优先级

1. 客户首次进入且未展示欢迎词：发送欢迎词。
2. 客户发送文本后：按优先级从高到低匹配已启用关键词。
3. 精确匹配优先于包含匹配；同级按配置的优先级排序。
4. 一条客户消息最多触发一条关键词回复，防止刷屏。
5. 快捷回复仅作为坐席输入辅助，不自动发送。

## 5. 推荐技术架构

### 5.1 技术栈

建议采用以下组合：

- 后端：PHP 8.3 + Laravel 12。
- 管理端与用户端：Vue 3 + TypeScript + Vite + Pinia。
- 客户端：Vue 3 响应式单页应用。
- 数据库：MySQL 8.0。
- 缓存与队列：Redis。
- 实时通信：Laravel Reverb/WebSocket。
- 异步任务：Laravel Queue，处理邮件通知、附件扫描和统计聚合。
- 文件存储：兼容 S3 的对象存储；本地开发可使用本地磁盘。
- Web 服务：Nginx。

如果需要严格遵循原生 PHP 目录结构，也可实现，但实时消息、队列、租户中间件和权限控制使用 Laravel 更稳妥。

### 5.2 系统组件

```text
浏览器（管理员后台 / 租户后台 / 客户端）
        │ HTTPS / WSS
        ▼
Nginx / API 网关
        ├── Laravel REST API
        ├── WebSocket 服务
        └── 静态前端资源
              │
       ┌──────┼────────┐
       ▼      ▼        ▼
     MySQL   Redis   对象存储
              │
              ▼
          Queue Worker
              ├── 邮件通知
              ├── 附件处理
              └── 统计任务
```

### 5.3 多租户隔离

- 租户业务表统一包含 `tenant_id` 并建立联合索引。
- 登录租户后台后，由中间件从服务端会话或令牌解析当前租户，禁止客户端自行指定有效租户身份。
- ORM 使用全局租户作用域；新增数据时自动写入 `tenant_id`。
- 通过客服链接访问时，由 `public_token` 解析 `channel_id` 和 `tenant_id`。
- WebSocket 频道必须包含租户边界并在服务端鉴权，例如 `private-tenant.{tenantId}.conversation.{conversationId}`。
- 平台管理员跨租户查询必须走独立管理服务和审计日志。
- 缓存键、队列任务和对象存储目录都必须包含租户标识。

## 6. 核心数据模型

所有主键建议使用 ULID，时间统一按 UTC 保存，前端按用户时区展示。

### 6.1 平台和租户

#### `platform_admins`

- `id`
- `username`
- `password_hash`
- `email`
- `role`
- `status`
- `last_login_at`
- `created_at`、`updated_at`

#### `tenants`

- `id`
- `name`
- `status`
- `plan_id`
- `owner_user_id`
- `expires_at`
- `created_at`、`updated_at`

#### `tenant_users`

- `id`
- `tenant_id`
- `username`
- `phone`
- `email`
- `password_hash`
- `display_name`
- `role`：`owner/admin/agent`
- `avatar_url`
- `status`
- `last_login_at`
- `created_at`、`updated_at`

唯一索引建议：`tenant_id + username`、`tenant_id + phone`。

### 6.2 接入渠道与配置

#### `channels`

- `id`
- `tenant_id`
- `name`
- `public_token`：随机高熵且全局唯一
- `brand_name`
- `brand_color`
- `avatar_url`
- `welcome_message`
- `offline_message`
- `assignment_mode`：首期为 `manual`
- `status`
- `created_by`
- `created_at`、`updated_at`

#### `channel_agents`

- `id`
- `tenant_id`
- `channel_id`
- `agent_id`
- `created_at`

唯一索引：`channel_id + agent_id`。

#### `keyword_replies`

- `id`
- `tenant_id`
- `channel_id`
- `keyword`
- `match_type`：`exact/contains`
- `reply_content`
- `priority`
- `status`
- `created_at`、`updated_at`

#### `quick_replies`

- `id`
- `tenant_id`
- `channel_id`，允许为空表示租户通用
- `title`
- `content`
- `sort_order`
- `status`
- `created_by`
- `created_at`、`updated_at`

### 6.3 客户身份

#### `customers`

- `id`
- `tenant_id`
- `channel_id`
- `phone_normalized`
- `phone_masked`
- `password_hash`
- `email`
- `email_verified_at`，首期可为空且不强制验证
- `register_ip`
- `register_user_agent`
- `fingerprint_hash`
- `last_login_ip`
- `last_login_at`
- `status`
- `created_at`、`updated_at`

唯一索引：`channel_id + phone_normalized`。

说明：原始浏览器指纹不落库，只保存带服务端密钥的 HMAC 哈希，减少隐私风险。

#### `customer_login_logs`

- `id`
- `tenant_id`
- `channel_id`
- `customer_id`，登录失败时可为空
- `phone_masked`
- `ip`
- `fingerprint_hash`
- `result`
- `failure_reason`
- `created_at`

### 6.4 会话与消息

#### `conversations`

- `id`
- `tenant_id`
- `channel_id`
- `customer_id`
- `assigned_agent_id`
- `status`：`waiting/active/closed`
- `last_message_id`
- `last_message_at`
- `customer_unread_count`
- `agent_unread_count`
- `accepted_at`
- `closed_at`
- `created_at`、`updated_at`

索引建议：

- `tenant_id + status + last_message_at`
- `channel_id + customer_id + status`
- `assigned_agent_id + status + last_message_at`

#### `messages`

- `id`
- `tenant_id`
- `conversation_id`
- `sender_type`：`customer/agent/system/bot`
- `sender_id`，系统消息可为空
- `message_type`：`text/image/file/system`
- `content`
- `attachment_id`
- `client_message_id`：客户端幂等 ID
- `created_at`

唯一索引：`conversation_id + client_message_id`。

#### `conversation_events`

- `id`
- `tenant_id`
- `conversation_id`
- `event_type`：`created/accepted/transferred/closed/reopened`
- `operator_type`
- `operator_id`
- `payload_json`
- `created_at`

#### `attachments`

- `id`
- `tenant_id`
- `uploader_type`
- `uploader_id`
- `storage_path`
- `original_name`
- `mime_type`
- `size`
- `status`
- `created_at`

## 7. API 设计

统一返回格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "request_id": "01J..."
}
```

错误使用合适的 HTTP 状态码；`code` 用于稳定表达业务错误。接口不得返回 SQL、堆栈、密码哈希或内部路径。

### 7.1 平台管理员 API

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/api/admin/auth/login` | 管理员登录 |
| POST | `/api/admin/auth/logout` | 退出登录 |
| GET | `/api/admin/dashboard` | 平台统计 |
| GET | `/api/admin/tenants` | 租户列表 |
| GET | `/api/admin/tenants/{id}` | 租户详情 |
| PATCH | `/api/admin/tenants/{id}/status` | 启用或禁用租户 |
| GET | `/api/admin/audit-logs` | 审计日志 |
| PUT | `/api/admin/settings` | 系统配置 |

### 7.2 租户用户 API

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/api/user/auth/register` | 租户注册 |
| POST | `/api/user/auth/login` | 租户或坐席登录 |
| GET | `/api/user/me` | 当前账号与租户信息 |
| GET/POST | `/api/user/agents` | 坐席列表/创建坐席 |
| PATCH | `/api/user/agents/{id}` | 更新坐席 |
| GET/POST | `/api/user/channels` | 渠道列表/创建渠道 |
| GET | `/api/user/channels/{id}/link` | 获取客服链接 |
| POST | `/api/user/channels/{id}/rotate-token` | 重置客服链接 |
| GET/POST | `/api/user/channels/{id}/keyword-replies` | 关键词回复配置 |
| GET/POST | `/api/user/quick-replies` | 快捷回复配置 |
| GET | `/api/user/conversations` | 会话列表 |
| GET | `/api/user/conversations/{id}` | 会话详情 |
| GET | `/api/user/conversations/{id}/messages` | 分页获取消息 |
| POST | `/api/user/conversations/{id}/accept` | 接入会话 |
| POST | `/api/user/conversations/{id}/messages` | 坐席发送消息 |
| POST | `/api/user/conversations/{id}/transfer` | 转接坐席 |
| POST | `/api/user/conversations/{id}/close` | 结束会话 |

### 7.3 客户端 API

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/api/client/channels/{token}` | 获取公开渠道展示配置 |
| POST | `/api/client/channels/{token}/auth` | 登录或自动注册 |
| POST | `/api/client/profile/email` | 首次补充邮箱 |
| GET | `/api/client/conversation` | 获取当前会话 |
| GET | `/api/client/conversation/messages` | 获取历史消息 |
| POST | `/api/client/conversation/messages` | 客户发送消息 |
| POST | `/api/client/auth/logout` | 退出当前渠道账号 |

### 7.4 WebSocket 事件

| 事件 | 接收方 | 内容 |
|---|---|---|
| `conversation.created` | 有渠道权限的坐席 | 新待接入会话 |
| `conversation.updated` | 客户与相关坐席 | 状态、分配和未读数变化 |
| `message.created` | 会话双方 | 新消息 |
| `agent.accepted` | 客户 | 客服已接入及客服展示信息 |
| `conversation.closed` | 客户与坐席 | 会话已结束 |
| `typing.updated` | 会话双方 | 可选的输入状态，首期可不实现 |

所有事件发送前再次校验频道订阅者是否属于对应租户、渠道或客户身份。

## 8. 页面与交互设计

### 8.1 管理员后台页面

- 登录页。
- 仪表盘。
- 租户管理和租户详情。
- 套餐管理。
- 风控记录。
- 系统配置。
- 管理员及审计日志。

### 8.2 租户用户后台页面

- 注册、登录、找回密码。
- 工作台。
- 客服坐席管理。
- 接入渠道列表。
- 客服配置：品牌、欢迎词、关键词回复、快捷回复、客服链接。
- 消息中心：会话列表、聊天窗口、客户资料侧栏。
- 操作日志和个人资料。

桌面端消息中心建议三栏布局：

```text
会话筛选/列表 | 消息记录与输入框 | 客户资料/会话操作
```

手机端改为列表与聊天窗口分层导航，保留全部核心操作。

### 8.3 客户端页面

- 渠道加载和无效链接提示页。
- 手机号、密码、验证码登录页。
- 首次邮箱补全弹窗。
- 聊天页面：品牌头部、消息区、接入状态、输入区。
- 移动端优先，同时兼容桌面浏览器。

### 8.4 关键交互

- 表单均异步提交，并显示加载、成功和错误状态。
- 客服链接复制后显示明确反馈。
- 消息发送采用 `client_message_id`，断网重试不得产生重复消息。
- WebSocket 断线后自动重连，并按最后消息 ID 补拉遗漏消息。
- 未接入时客户端显示“等待客服接入”；接入后插入不可伪造的系统消息。

## 9. 安全与隐私设计

### 9.1 身份与会话安全

- 全站仅通过 HTTPS/WSS 提供服务。
- 密码使用 Argon2id 或 bcrypt；不得记录、回传或通过日志输出密码。
- 管理员、租户用户和客户使用三套独立认证守卫与 Cookie 名称。
- Cookie 设置 `HttpOnly`、`Secure`、`SameSite=Lax/Strict`。
- 登录、注册、重置密码必须有后端验证码和频率限制。
- 管理员建议启用 TOTP 二次验证。
- 敏感操作重新验证身份并记录审计日志。

### 9.2 访问控制

- 每次读取、修改会话时同时校验 `tenant_id`、渠道权限和会话归属。
- 客户只能访问令牌对应渠道下自己的数据。
- 禁止通过传入其他 `tenant_id`、`customer_id` 或 `conversation_id` 越权访问。
- 客服链接使用至少 128 位随机熵，不使用可递增 ID。
- 重置客服链接时撤销旧令牌并记录操作者。

### 9.3 IP 与浏览器指纹

- 注册 IP 从可信反向代理链解析，只有配置过的代理头可被信任。
- 指纹用于风险识别，不作为唯一登录凭证。
- 前端采集稳定且必要的浏览器特征后生成指纹，后端使用服务端密钥再次执行 HMAC 后保存。
- 在客户页面提供隐私告知，说明收集 IP、设备特征、用途和保存期限。
- 根据业务所在地遵守适用的个人信息保护法规，并提供数据删除和导出机制。

### 9.4 输入、消息与附件

- 所有文本输出进行 HTML 转义；首期消息只存纯文本，不允许任意 HTML。
- 数据库使用 ORM 或预处理语句。
- 文件上传校验扩展名、MIME、文件大小和文件内容，随机重命名并禁止脚本执行。
- 私有附件通过鉴权后的临时签名 URL 访问。
- 对消息发送、关键词触发和附件上传执行限流。
- 对手机号、邮箱等敏感字段在后台列表默认脱敏显示。

## 10. 非功能要求

### 10.1 性能

- 会话列表使用游标分页，按 `last_message_at` 倒序。
- 消息历史使用消息 ID 游标向前分页，避免大偏移量查询。
- 在线状态、短期未读状态和频控计数放入 Redis。
- 消息必须先持久化数据库，再发布 WebSocket 事件。
- 统计报表通过异步聚合，不在消息主链路执行复杂统计。

### 10.2 可用性

- WebSocket 不可用时，前端降级为定时增量拉取。
- 队列任务必须幂等并设置有限重试和失败队列。
- 每条请求生成 `request_id`，用于日志追踪。
- 数据库定期备份，并验证恢复流程。

### 10.3 日志

必须记录：

- 平台管理员、租户用户和客户登录结果。
- 坐席、渠道、自动回复配置的增删改。
- 会话接入、转接、关闭和重新打开。
- 客服链接令牌重置。
- 风控拦截及权限拒绝。

日志不得包含密码、验证码、会话令牌、完整敏感配置和消息附件密钥。

## 11. 开发阶段规划

### 第一阶段：基础平台与租户体系

- 项目基础架构、数据库、Redis 和统一响应。
- 平台管理员登录与租户管理。
- 租户注册登录、租户隔离和角色权限。
- 坐席管理、渠道创建和客服链接。

验收：两个租户之间无法通过接口或 WebSocket 访问对方数据。

### 第二阶段：客户认证与实时聊天

- 客户链接解析。
- 手机号密码登录和自动注册。
- 注册 IP、User-Agent、指纹哈希记录。
- 首次邮箱弹窗。
- 会话、消息、未读数和 WebSocket 实时推送。
- 坐席接入及客户端“客服已接入”事件。

验收：客户可从专属链接注册进入；坐席实时看到消息并接入；刷新后历史消息完整。

### 第三阶段：客服自动化与管理能力

- 欢迎词。
- 关键词回复。
- 快捷回复。
- 会话转接、关闭、重新打开。
- 操作日志、登录日志和基础统计。

验收：各渠道配置独立生效，自动回复只在对应渠道触发。

### 第四阶段：安全、容量与上线

- 验证码、限流、风控和管理员二次验证。
- 附件安全、对象存储和签名访问。
- 队列、监控、告警、备份和恢复。
- 权限测试、并发接入测试、跨租户越权测试。
- 响应式兼容和部署配置。

验收：通过关键安全测试、并发测试和备份恢复演练后方可上线。

## 12. 测试方案

### 12.1 功能测试

- 同一渠道首次手机号登录自动注册。
- 同一渠道已有手机号必须使用正确密码登录。
- 同一手机号在不同渠道形成独立账号。
- 邮箱为空时不能绕过首次补全弹窗发送消息。
- 欢迎词只按配置规则展示。
- 精确关键词和包含关键词按优先级正确触发。
- 坐席接入后客户端立即显示系统消息。
- 快捷回复可插入并发送，且不自动触发。
- 会话转接、结束和重新打开状态正确。

### 12.2 权限与安全测试

- 租户 A 无法访问租户 B 的坐席、渠道、客户、会话和附件。
- 坐席无法访问未授权渠道。
- 客户无法通过修改 ID 访问其他客户消息。
- WebSocket 私有频道订阅必须鉴权。
- 客服链接旧令牌重置后失效。
- 登录、注册和消息接口限流有效。
- XSS、SQL 注入、CSRF、越权和恶意附件测试。

### 12.3 并发与可靠性测试

- 两个坐席同时领取一个会话，只允许一个成功。
- 消息重复提交只保存一次。
- WebSocket 断线重连后不丢消息、不重复展示。
- 队列任务重复执行不产生重复通知。
- 大量会话下分页和未读数更新保持稳定。

### 12.4 响应式测试

至少覆盖 `360px`、`390px`、`768px`、`1024px` 和 `1440px` 视口，验证登录、邮箱弹窗、会话列表、聊天和配置页面。

## 13. 部署建议

### 13.1 服务组成

- Nginx。
- PHP-FPM/Laravel API 服务。
- WebSocket 服务进程。
- Queue Worker 和定时任务进程。
- MySQL 主库。
- Redis。
- 对象存储。
- 日志、指标和错误监控系统。

### 13.2 环境隔离

开发、测试和生产环境使用独立数据库、Redis、存储桶、域名和密钥。生产密钥仅通过环境变量或密钥管理服务注入，不提交到代码仓库。

### 13.3 上线检查

- 数据库迁移和回滚脚本已验证。
- HTTPS 证书、可信代理和 CORS 来源配置正确。
- 队列与 WebSocket 由进程管理器守护。
- 定时备份和恢复验证已启用。
- 管理员初始密码已修改并启用二次验证。
- 日志不包含密码、令牌和隐私明文。

## 14. 首期不建议纳入的功能

为控制首期复杂度，以下功能建议后续迭代：

- AI 智能客服和知识库检索。
- 多语言自动翻译。
- 语音和视频通话。
- 微信、WhatsApp 等第三方渠道聚合。
- 复杂排队、技能组和智能分配。
- 客户跨渠道统一身份。
- 完整 CRM 和营销自动化。

## 15. 关键验收标准

1. 平台管理员可以管理租户，但不能获取任何账号密码明文。
2. 租户可以创建坐席和渠道，并获得不可枚举的专属客服链接。
3. 客户账号严格限定在当前客服链接对应渠道内。
4. 客户首次手机号访问可自动注册，已有账号必须校验密码。
5. 自动注册准确记录注册 IP、User-Agent 和指纹哈希。
6. 客户首次进入聊天必须填写邮箱，且服务端不可绕过。
7. 坐席可在消息列表进入会话并原子接入，客户实时看到“客服已接入”。
8. 欢迎词、关键词回复和快捷回复均可按渠道配置并正确生效。
9. 消息持久化、实时推送、断线恢复和幂等机制正常。
10. 任何 HTTP、WebSocket、缓存、队列和附件访问均不得发生跨租户越权。
